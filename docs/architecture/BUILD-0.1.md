# BUILD 0.1 — Repository Foundation

Creates the reproducible UAF monorepo and package shells only. Supabase, PowerSync, auth, domain logic, PWA and deployment are deliberately deferred to their build epics.
